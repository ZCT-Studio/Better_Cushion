{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${name}",
  "description": "${description}",
  "authors": [${authors}],
  "contributors": [${contributors}],
  "contact": {
    "homepage": "https://modrinth.com/mod/Better_Cushion",
    "sources": "https://github.com/ZCT-Studio/Better_Cushion",
    "issues": "https://github.com/ZCT-Studio/Better_Cushion/issues"
  },
  "license": "${license}",
  "icon": "icon.png",
  "environment": "*",
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {
    "fabricloader": "*",
    "fabric-api": "*",
    "minecraft": "${semver_mc_version_range}",
    "cushionbackport": "${cushionsbackport_version}"
  }
}
